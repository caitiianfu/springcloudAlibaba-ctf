spring:
  profiles:
    active: dev
  application:
    name: tian-sentinel
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8899
      config:
        server-addr: localhost:8899
        file-extension: yaml