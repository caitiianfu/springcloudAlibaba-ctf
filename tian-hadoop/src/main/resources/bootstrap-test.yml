spring:
  profiles:
    active: dev
  application:
    name: tian-hadoop
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8899
      config:
        server-addr: localhost:8899
        file-extension: yaml