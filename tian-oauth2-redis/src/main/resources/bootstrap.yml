spring:
  profiles:
    active: dev
  application:
    name: tian-oauth2-redis
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8899
      config:
        server-addr: localhost:8899
        file-extension: yaml